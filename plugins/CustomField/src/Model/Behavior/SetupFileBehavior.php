<?php
namespace CustomField\Model\Behavior;

use CustomField\Model\Behavior\SetupBehavior;

class SetupFileBehavior extends SetupBehavior {
	public function initialize(array $config) {
        parent::initialize($config);
    }
}
